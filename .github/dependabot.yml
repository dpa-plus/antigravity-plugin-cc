version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
    # One grouped PR for all action bumps (avoids conflicting per-action PRs).
    groups:
      github-actions:
        patterns:
          - "*"
